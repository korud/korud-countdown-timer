# 2.0
-Change the hardware, now use arduino nano con lcd keypad shield for arduino with 6 buttons.

-The code have the base but is incompatible with the before hardware.

# 1.1
-Add the countdown visual

-Add countdown previous
# 1.0
The first code, This is the code basic of proyect. I only have a spare parts of my printer 3d so I use arduino mega, ramps 1.4 and 2 basic buttons with one resistor each one for control the menu and options.
